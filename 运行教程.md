# PowerFit Gym 健身管理系统 — 运行教程

## 📋 环境要求

| 软件 | 版本要求 | 说明 |
|---|---|---|
| JDK | 8 或以上 | Java 运行环境 |
| MySQL / MariaDB | 5.7+ / 10.x | 数据库 |
| Maven | 3.6+ | 打包工具（可选，项目内嵌了 IDEA Maven） |
| IntelliJ IDEA | 任意版本 | 开发工具（可选） |

---

## 🚀 快速启动（3 步）

### 第 1 步：导入数据库

**推荐用 SQLyog** 直接导入 `sql/gym_db_full.sql`（含建库+数据一条龙）：
1. 打开 SQLyog，连接本地 MySQL
2. 右键 `root@localhost` → 导入 → 执行 SQL 脚本
3. 选择 `sql/gym_db_full.sql` → 执行

或者命令行方式：

```bash
# 方式A：一键导入完整数据库（推荐）
mysql -u root -p < sql/gym_db_full.sql

# 方式B：分步执行
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS gym_db DEFAULT CHARACTER SET utf8mb4;"
mysql -u root -p gym_db < sql/init.sql
mysql -u root -p gym_db < sql/add_gym_capacity.sql
```

> 数据库连接信息在 `src/main/resources/application.yml` 里：
> - 地址：`localhost:3306`
> - 库名：`gym_db`
> - 用户名：`root`
> - 密码：`1234`
>
> 如果你的 MySQL 密码不同，**改一下 application.yml 里的 password**。

---

### 第 2 步：打包编译

**方式 A：用 IntelliJ IDEA（推荐）**
1. 用 IDEA 打开项目根目录
2. 右侧 Maven 面板 → 双击 `clean` → 再双击 `package`
3. 跳过测试可在 `package` 前点一下闪电图标

**方式 B：命令行**
```bash
mvn clean package -DskipTests
```
打包成功后 jar 在 `target/gym-management-1.0.0.jar`。

> 如果你的 JAVA_HOME 没配，在 IDEA 的 Terminal 里跑，IDEA 自带了 JDK。

---

### 第 3 步：启动运行

```bash
java -jar target/gym-management-1.0.0.jar
```

启动后访问：
```
http://localhost:8765
```

---

## 👥 默认账号

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员 | admin | admin123 |
| 会员用户 | 在登录页注册 | — |

---

## 📱 功能模块

### 会员端 (`localhost:8765` → 登录)

| 模块 | 功能 |
|---|---|
| 📅 每日打卡 | 签到打卡、连续天数、成就徽章 |
| 🏋️ 训练计划 | 增肌/减脂/自定义计划、周视图、动作库 |
| 💳 会员卡 | 购卡、卡片管理 |
| 👨‍🏫 教练团队 | 浏览教练信息 |
| 🏢 场馆状态 | 在场人数、入场统计、**器械占用率**、**空闲器械**、峰值折线图 |
| 📰 通知公告 | 健身房新闻 |
| 📊 身体数据 | BMI、体脂率记录追踪 |
| 🛒 商城 | 商品购买 |

### 管理端 (`localhost:8765/admin.html`)

| 模块 | 功能 |
|---|---|
| 📊 数据概览 | 会员数、营收、峰值人数 |
| 🚪 会员入场 | 手机号入场/退场、当前在场人员 |
| 👥 会员管理 | 查看/编辑会员信息 |
| 👨‍🏫 教练管理 | 教练增删改 |
| 🛒 商品管理 | 商品上下架 |
| 📰 新闻管理 | 发布公告 |
| 💰 销售统计 | 日营收图表 |
| ✅ 打卡总览 | 全站打卡概览 |

---

## 🔧 端口修改

默认端口 `8765`，要改的话编辑 `application.yml`：

```yaml
server:
  port: 8765   # 改成你想要的端口
```

---

## ⚠️ 常见问题

**Q: 启动报 "Access denied for user"**
A: 你的 MySQL 密码不是 `1234`，改 `application.yml` 里的 password。

**Q: 启动报 "Unknown database 'gym_db'"**
A: 没创建数据库，先执行第 1 步的建库 SQL。

**Q: 端口 8765 被占用**
A: 换端口或在任务管理器里结束旧的 java 进程。

**Q: 打包失败提示 lombok 或编译错误**
A: 检查 JDK 版本是否 8+，Maven 版本是否 3.6+。

---

> 📦 完整代码 + 数据库脚本 + 教程，打包于 2026.06
